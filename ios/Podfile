target 'Runner' do
  use_frameworks! :linkage => :static      # see §2
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  flutter_additional_ios_build_settings(installer)   # <‑‑ REQUIRED
end
